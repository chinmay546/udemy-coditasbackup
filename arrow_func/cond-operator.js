const myAge = 16;
let msg;
msg = myAge >= 18 ? "yes vote" : "no vote";
console.log(msg);

const team = ["Array", "Chinmay", "gre", "wergr", "srh"];

const outputmsg =
  team.length <= 4 ? `Team Size is ${team.length}` : "Too many people on team";
console.log(outputmsg);
